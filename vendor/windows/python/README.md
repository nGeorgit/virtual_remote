# Vendored Python Runtime Placeholder

Drop the authoritative Windows build Python runtime here as `python.exe` plus the rest of the CPython 3.8.x directory tree.

The build script expects the interpreter at `vendor/windows/python/python.exe` and verifies that it reports Python 3.8 before building.
